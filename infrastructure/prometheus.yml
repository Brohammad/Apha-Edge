global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: alphaedge-api
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["api:8000"]
    # In production, add bearer_token or basic_auth here.
    # bearer_token: <METRICS_API_KEY>
