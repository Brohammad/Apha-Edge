[build-system]
requires = ["setuptools>=68", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "alphaedge-cpp"
version = "0.1.0"
description = "C++ backtest acceleration module for AlphaEdge (optional)"
requires-python = ">=3.12"
